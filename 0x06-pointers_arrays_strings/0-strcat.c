#include "main.h"

/**
 * *_strcat - concatenates two strings
 * @dest: Destination string
 * @src: Source string
 * Return: dest
 */

char *_strcat(char *dest char *src)
{
	int i, j;
	strcat(dest[i],src[j]);
	dest[i] = '\0';
	return(dest);
}
